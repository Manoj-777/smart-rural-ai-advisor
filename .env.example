# ============================================================
# Smart Rural AI Advisor — Environment Variables
# ============================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to Git — it contains secrets.
# ============================================================

# AWS Region
AWS_REGION=ap-south-1

# OpenWeatherMap
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here

# DynamoDB Table Names (must match SAM template.yaml)
DYNAMODB_PROFILES_TABLE=farmer_profiles
DYNAMODB_SESSIONS_TABLE=chat_sessions

# S3 Bucket (created during setup)
S3_BUCKET_NAME=smart-rural-ai-data-YOUR_ACCOUNT_ID

# Bedrock AgentCore
BEDROCK_AGENT_ID=your_agent_id_here
BEDROCK_AGENT_ALIAS_ID=your_agent_alias_id_here
BEDROCK_KB_ID=your_knowledge_base_id_here

# Bedrock Model
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-5-20250514

# API Gateway (filled after sam deploy)
API_GATEWAY_URL=https://YOUR_API_ID.execute-api.ap-south-1.amazonaws.com/prod

# Frontend (React — used by Vite)
VITE_API_URL=https://YOUR_API_ID.execute-api.ap-south-1.amazonaws.com/prod
